# Agentic Spectrum Detect - Environment Configuration
# Copy this file to .env and fill in your values
# SECURITY: Never commit .env files with real credentials!

# =============================================================================
# LLM Configuration (REQUIRED)
# =============================================================================
# Your API key for the LLM service - MUST be set, no default
LLM_API_KEY=your-api-key-here

# LLM API endpoint
LLM_API=http://vllm:8888/v1

# Model name to use
LLM_MODEL=gpt-oss-20b

# Reasoning level: low, medium, high
REASONING_LEVEL=low

# =============================================================================
# Spectrum Server Configuration
# =============================================================================
# MCP server address for the spectrum server
SPECTRUM_SERVER_MCP_SERVER=http://spectrum_server:8000
SPECTRUM_SERVER_MCP_ROUTE=/llm/mcp/

# SDR Backend: uhd (for Ettus B210), soapy, or simulated
SDR_BACKEND=uhd

# SDR device serial number (optional - leave empty for auto-detect)
SDR_DEVICE_SERIAL=

# =============================================================================
# Spectrum Core GPU Service
# =============================================================================
# ZeroMQ endpoint for GPU FFT service
SPECTRUM_CORE_ENDPOINT=tcp://spectrum_core:5555
SPECTRUM_CORE_BIND=tcp://*:5555
SPECTRUM_CORE_RESPONSE_TIMEOUT_MS=200

# =============================================================================
# Dashboard Configuration
# =============================================================================
# Detection threshold in dB for dashboard overlay
DASHBOARD_DETECTION_THRESHOLD_DB=-65

# =============================================================================
# Network Configuration
# =============================================================================
# Hostname for the services
HOST_NAME=localhost

# =============================================================================
# Agent Configuration
# =============================================================================
# System prompt for the spectrum analyzer agent
SYSTEM_PROMPT="You are an expert spectrum analyzer scientific measurement assistant. You can tune a receiver to different parts of the spectrum and detect intermittent or continuous energy. You can determine the RSSI measurement of the energy as well as the spectrogram_diff score of the overall captured bandwidth."
